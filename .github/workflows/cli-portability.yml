name: CLI portability

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  portability:
    uses: ORESoftware/.github/.github/workflows/reusable-go-cli-portability.yml@89974ffcd5f6848bfdf5117e3edf979c2dcf8a03
    with:
      working-directory: .
      main-package: ./cmd/jlc
      run-smoke: true
